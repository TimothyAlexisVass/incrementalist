The quest completion announcement has the wrong trigger.
Now it's triggered when the user CLAIMS it, it must trigger when the user completes the REQUIREMENTS of it, meaning, the announcement must come when it's READY to be CLAIMED, not after the user clicks the CLAIM button.
